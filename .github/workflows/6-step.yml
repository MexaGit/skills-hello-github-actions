name: Step 6

on:
  push:
    branches:
      - main

jobs:
  start_exercise:
    name: "My first action in the github worflow"
    uses: MexaGit/exercise-toolkit/.github/workflows/6-step.yml@1.0.0
    with:
      excecise-title: "Github Actions"
      intro-message: "Hi all"
    
